{
  "schema_version": "1.0",
  "name": "leo-core",
  "description": "LEO Core MCP server for AI Visibility Scoring",
  "tools": [
    {
      "name": "leo_audit",
      "description": "Run a full audit for a given URL and return structured results",
      "parameters": {
        "url": "string"
      }
    },
    {
      "name": "leo_recent",
      "description": "Fetch the 10 most recent audit scores from the database",
      "parameters": {}
    }
  ],
  "port": 8800,
  "entrypoint": "leo.mcp.server:start_mcp_server"
}
