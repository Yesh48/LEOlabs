apiVersion: v1
kind: Service
metadata:
  name: {{ include "leo-core.fullname" . }}
  labels:
    {{- include "leo-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "leo-core.selectorLabels" . | nindent 4 }}
